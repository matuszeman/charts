global:
  cluster: test

app:
  appName: test
  appVersion: 1.0.0

  deployment:
    enabled: true

  containers:
    app:
      image: test
      imageTag: 1.0.0
      resources:
        requests:
          cpu: 100m
          memory: 10Mi
      configDirs:
        app: /config/app

  configs:
    app:
      content:
        file1: file1 content
        plaintextkey: sometext