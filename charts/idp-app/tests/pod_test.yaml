suite: pod
templates:
  - deployment.yaml
values:
  - values/minimum.yaml
tests:
  - it: when .volumeFiles are set, then generate container volume mount
    values:
      - values/deployment.yaml
    set:
      containers:
        app:
          volumeFiles:
            pvcvol:
              file.yaml:
                mountPath: /contfile.yaml

      volumes:
        pvcvol:
          persistentVolumeClaim:
            claimName: pvc-files
    asserts:
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            mountPath: /contfile.yaml
            name: pvcvol
            readOnly: true
            subPath: file.yaml

  - it: should use existing PVC when claimName is specified
    values:
      - values/deployment.yaml
    set:
      volumes:
        data:
          persistentVolumeClaim:
            claimName: existing-pvc
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data
            persistentVolumeClaim:
              claimName: existing-pvc

  - it: should reference auto-created PVC when claimName is not specified
    values:
      - values/deployment.yaml
    set:
      volumes:
        data:
          persistentVolumeClaim:
            accessMode: ReadWriteOnce
            size: 10Gi
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data
            persistentVolumeClaim:
              claimName: RELEASE-NAME-data

  - it: should reference auto-created PVC with multi deployment
    values:
      - values/deployment.yaml
      - values/multi.yaml
    set:
      volumes:
        data:
          persistentVolumeClaim:
            accessMode: ReadWriteOnce
            size: 10Gi
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data
            persistentVolumeClaim:
              claimName: RELEASE-NAME-one-data
        documentIndex: 0
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data
            persistentVolumeClaim:
              claimName: RELEASE-NAME-two-data
        documentIndex: 1

  - it: should handle mixed volume types correctly
    values:
      - values/deployment.yaml
    set:
      volumes:
        data:
          persistentVolumeClaim:
            accessMode: ReadWriteOnce
            size: 10Gi
        existing-pvc:
          persistentVolumeClaim:
            claimName: my-existing-pvc
        config:
          configMap:
            name: app-config
    asserts:
      - contains:
          path: spec.template.spec.volumes
          content:
            name: data
            persistentVolumeClaim:
              claimName: RELEASE-NAME-data
      - contains:
          path: spec.template.spec.volumes
          content:
            name: existing-pvc
            persistentVolumeClaim:
              claimName: my-existing-pvc
      - contains:
          path: spec.template.spec.volumes
          content:
            name: config
            configMap:
              name: app-config

  - it: when .securityContext is set, then generate pod security context
    values:
      - values/deployment.yaml
    set:
      securityContext:
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000
        supplementalGroups: [ 4000 ]
    asserts:
      - equal:
          path: spec.template.spec.securityContext
          value:
            runAsUser: 1000
            runAsGroup: 3000
            fsGroup: 2000
            supplementalGroups: [ 4000 ]

  - it: when idpAppConfig.defaults.securityContext is set, then use it as default pod security context
    values:
      - values/deployment.yaml
    set:
      global.idpAppConfig.defaults.securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
    asserts:
      - equal:
          path: spec.template.spec.securityContext
          value:
            runAsNonRoot: true
            runAsUser: 1000
            fsGroup: 1000

  - it: when both idpAppConfig.defaults.securityContext and .securityContext are set, local overrides default
    values:
      - values/deployment.yaml
    set:
      global.idpAppConfig.defaults.securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      securityContext:
        runAsUser: 2000
        fsGroup: 3000
    asserts:
      - equal:
          path: spec.template.spec.securityContext
          value:
            runAsNonRoot: true
            runAsUser: 2000
            fsGroup: 3000

